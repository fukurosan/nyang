# Grid
You can enable a background grid on the graph canvas by supplying the enableGrid attribute to nyang's options object.

Below is an example:
```javascript
const options = {enableGrid: true}
const graph = new NYANG(graphContainer, data, options)
```
